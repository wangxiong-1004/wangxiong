let isDone: boolean = true;
let num: number = 10;
let names: string = 'wng';
let str: string = `this name is ${names}`;
let list: number[] = [1, 2, 3];
let list2: Array<number> = [1, 2, 3];
let x: [string, number];
x = ['string', 10];

enum Color {
    Red,
    Green,
    Blue
}

let c: Color = Color.Blue;